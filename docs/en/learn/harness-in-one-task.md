# Harness in One Task

## What this document helps you do

This document teaches Harness through two concrete task flows before you read the stricter reference definitions.

After reading it, you should be able to recognize why Harness records a Task, a Change Unit, a Decision Packet, Approval, Write Authorization, Evidence, Verification, Manual QA, Acceptance, Residual Risk, and Close. You do not need to know the internal record details to follow the story.

## Read this when

Read this when you want to understand Harness through concrete task flow before learning the stricter terms.

## Before you read

[Overview](overview.md) is recommended first, but not required. This page assumes only that you know Harness keeps durable work facts outside the chat.

## Main idea

Harness is easiest to understand as a task story: name what is being attempted, bound what may change, ask for user judgment at the right time, record support, and close only when visible blockers are handled.

## Why examples come first

Harness can sound heavier than it feels in use. The strict reference docs need exact rules because they define how a local system records state and checks gates. A user, though, usually experiences Harness as a few practical questions:

1. What are we trying to do?
2. What is allowed to change?
3. What judgment is needed from the user?
4. What changed, and what proves it?
5. What is still uncertain?
6. Can this task close?

The examples below start there. They show how Harness keeps a small task light and gives a larger task enough structure to stay followable.

## Example A: Direct task

### User request: "Change the button label."

A user points at a small UI copy issue and says:

```text
Change the button label.
```

Assume the surrounding context makes the target obvious: a profile page button currently says "Save" and should say "Update profile."

### Intake

The agent starts by turning the request into a Task in plain language:

```text
Task: Change the profile page button label from "Save" to "Update profile."
Likely mode: direct.
```

The Task exists so the work has one durable unit. Without it, the change, the check, and the close decision would live only in chat.

### Scope

The agent narrows the scope before touching files:

```text
In scope: profile page button copy and any nearby snapshot or copy test that directly covers it.
Out of scope: redesigning the profile page, changing save behavior, changing localization strategy.
```

This matters even for a small task. If the agent discovers that the button text is generated by a shared design token, the task may no longer be a tiny copy edit.

### Direct classification

Harness treats this as `direct` because the change is small, low risk, and easy to self-check. Direct does not mean "no record." It means the record should stay lightweight.

If the agent finds the label is shared across checkout, billing, and profile screens, it should stop and move the same Task toward `work` because the impact is wider than expected.

### Minimal Change Unit

The Change Unit is the smallest product-write boundary that can complete the request:

```text
Change Unit: profile button copy only.
Expected paths: profile view file and the directly related copy test, if present.
```

The Change Unit prevents a tiny request from quietly becoming a general UI cleanup.

### prepare_write before product write

Before editing, the agent asks Harness whether this write is currently allowed. In user-facing language, the answer should be specific:

```text
Write Authorization: allowed for the profile view file and matching copy test.
```

If the intended file is outside the Change Unit, the write should pause. The user should see a scope question instead of a surprise edit.

### Recording the change

After editing, the agent records what happened:

```text
Changed: profile page button label.
Changed paths: profile view file; copy test if updated.
```

Recording the change connects the implementation to the Task and Change Unit. It also makes later status summaries possible without rereading the whole chat.

### Lightweight evidence

For direct work, evidence can be simple:

```text
Evidence: diff shows the label changed; copy test passed or component render was self-checked.
```

The point is not ceremony. The point is that "done" should be supported by something durable enough to inspect later.

### Self-check

The agent checks the narrow result:

```text
Self-check: the profile button now renders "Update profile."
```

A self-check is useful, but it is not independent verification. Direct tasks usually close with self-checked assurance unless the user asks for more.

### Close

Harness can close the Task when the scope stayed narrow, the write was authorized, the changed paths match the Change Unit, the lightweight evidence exists, and no close-relevant risk is known.

The close summary should be plain:

```text
Closed as direct, self-checked.
```

### What the user sees

The user should see a compact result, not internal machinery:

```text
Changed the profile button label to "Update profile."
Checked the rendered copy.
Closed as a small direct task.
```

### What the reference docs define more strictly

The reference docs define the exact rules behind this simple flow: how direct mode differs from work mode, what counts as an active Change Unit, how `prepare_write` creates Write Authorization, how changed paths are recorded, and when a direct task may close as self-checked.

This tutorial leaves those details out on purpose.

## Example B: Work task

### User request: "Add remember-me behavior to the login flow."

A user asks:

```text
Add remember-me behavior to the login flow.
```

This sounds simple, but it touches authentication behavior, session lifetime, UI copy, storage, tests, and possibly security policy. Harness should make the shape of the work visible before implementation.

### Intake

The agent starts a Task:

```text
Task: Add remember-me behavior to the login flow.
Likely mode: work.
```

The agent also asks the first useful question in ordinary language:

```text
Should "remember me" extend the login session on this device, prefill the email address, or both?
```

### Scope shaping

The agent proposes a first scope:

```text
In scope: login form checkbox, session persistence behavior, tests for selected behavior, user-facing copy.
Out of scope: passwordless login, account recovery, global session-management redesign.
```

This becomes the starting point for a Change Unit. The Change Unit is not just a list of files; it is the bounded product slice the agent may change to satisfy the Task.

### Trade-off appears

The agent discovers two reasonable interpretations:

1. Remember the user's email only. This is lower risk and improves convenience, but does not keep the user signed in.
2. Extend the session on the current device. This matches many users' expectation of "remember me," but needs a security decision about session lifetime and storage.

The agent should not quietly pick one if that choice changes product behavior or risk.

### Decision Packet

Harness uses a Decision Packet when user judgment blocks progress. The packet should be readable:

```text
Decision needed: What should "remember me" mean for this product?
Option A: remember email only.
Option B: extend the session on this device.
Recommendation: choose Option B only if the product accepts the session-lifetime risk and records that choice.
```

The Decision Packet exists because this is not mere approval to edit. It is a product and security trade-off. The user owns that choice.

### Change Unit

After the user chooses, the agent updates the Change Unit to match the decision:

```text
Change Unit: login form checkbox, selected remember-me behavior, tests, and directly related copy.
```

If the user chooses extended sessions, the Change Unit may include session persistence code and security-sensitive tests. If the user chooses email-only memory, it stays narrower.

### Write Authorization

Before product writes, the agent asks Harness to check the intended edit against the active Task, Change Unit, approvals, and resolved decisions.

The user-facing summary should say what is allowed and what is not:

```text
Write Authorization: allowed for login form, session persistence code, and related tests.
Not allowed: unrelated account recovery or global auth redesign.
```

If the chosen behavior requires a sensitive approval, Harness should stop before the write and ask for that approval separately. Approval answers "may this sensitive action proceed?" It does not replace the Decision Packet, tests, QA, risk acceptance, or final acceptance.

### Implementation

The agent implements inside the authorized boundary:

1. Add the checkbox and copy.
2. Add the selected persistence behavior.
3. Add or update tests for the chosen behavior.
4. Avoid unrelated auth cleanup unless a new scope decision allows it.

If the agent finds that the current session system cannot support the chosen behavior without a larger redesign, it should stop and propose a smaller Change Unit or a new Decision Packet.

### Evidence

Evidence ties claims to records:

```text
Evidence: diff for login form and session code; test output for remember-me behavior; notes from the implementation run.
```

Evidence is why the user can later ask, "What supports the claim that remember-me works?" and get a concrete answer instead of a chat memory.

### Verification

For work mode, Harness expects stronger checking than a direct self-check unless the user explicitly accepts the verification risk.

Useful verification might be:

```text
Detached verification: a separate check confirms that a remembered session survives browser restart and a non-remembered session does not.
```

If detached verification cannot run, the agent should say so plainly and show the remaining verification risk. Closing without it is a risk-accepted close, not detached verification.

### Manual QA

Manual QA asks whether a person inspected the experience:

```text
Manual QA: login screen shows the checkbox clearly, copy is understandable, keyboard and screen-reader flow remain acceptable, and the remembered-session behavior matches the chosen option.
```

Manual QA exists because tests can pass while the experience is still confusing, clipped, inaccessible, or surprising.

### Residual risk

Before acceptance or risk-accepted close, the agent shows known remaining uncertainty:

```text
Residual risk: session behavior was verified in the local browser path, but not across every supported browser or enterprise policy setting.
```

If there is no known close-relevant residual risk, the agent should say that plainly. Hiding risk and having no known risk are different.

### Acceptance

When final acceptance is required, the user accepts the result after seeing evidence, verification or accepted verification risk, Manual QA status, and residual risk.

```text
Accepted. The remember-me behavior matches the selected option and the shown residual risk is acceptable.
```

Acceptance is not the same as approval. Approval may let a sensitive step proceed; acceptance says the completed result is good enough.

### Close

Harness closes only after the relevant blockers are clear: scope is compatible, decisions are resolved or validly deferred, writes were authorized, evidence is sufficient for the Task, verification and QA are passed or explicitly handled, residual risk is visible, and required acceptance is recorded.

The close summary should be short:

```text
Closed as work. Evidence recorded. Verification and Manual QA handled. Residual risk shown and accepted where applicable.
```

### What the user sees

The user should see the work journey, not a reference manual:

```text
Implemented remember-me as extended session on this device.
Changed login form, session persistence, and tests.
Verification passed for remembered and non-remembered sessions.
Manual QA passed for the login screen flow.
Residual risk shown and accepted: not checked across every supported browser policy.
Accepted and closed as work.
```

### What the reference docs define more strictly

The reference docs define the exact mode rules, Decision Packet compatibility, approval handling, Write Authorization behavior, evidence sufficiency, verification independence, QA gates, residual-risk visibility, acceptance timing, and close semantics.

This tutorial only shows why those pieces exist.

## The same concepts in one table

| everyday phrase | Harness term | why it exists | where to read more |
|---|---|---|---|
| "What are we doing?" | Task | Keeps the user's desired outcome, status, blockers, evidence, and close decision connected. | [Use: User Guide](../use/user-guide.md); current owner: [Kernel Spec](../03-kernel-spec.md). Future reference path: `reference/kernel.md`. |
| "What may change?" | Change Unit | Bounds product writes so the task does not silently expand. | [Use: User Guide](../use/user-guide.md); current owner: [Kernel Spec](../03-kernel-spec.md). Future reference path: `reference/kernel.md`. |
| "This needs your call." | Decision Packet | Separates user-owned product judgment from broad approval. | [Use: User Guide](../use/user-guide.md); current owner: [Kernel Spec](../03-kernel-spec.md). Future reference path: `reference/kernel.md`. |
| "May this sensitive step proceed?" | Approval | Allows a sensitive action inside a defined scope without replacing product judgment or final acceptance. | Current owner: [Kernel Spec](../03-kernel-spec.md). Future reference path: `reference/kernel.md`. |
| "May I edit these files now?" | Write Authorization | Checks that the intended write fits the current Task, Change Unit, decisions, and approvals. | Current owners: [Kernel Spec](../03-kernel-spec.md) and [Agent Integration](../09-agent-integration.md). Future reference paths: `reference/kernel.md`, `reference/agent-integration.md`. |
| "Here is what supports the claim." | Evidence | Makes "done" inspectable through diffs, logs, checks, screenshots, or other records. | [Use: User Guide](../use/user-guide.md); current owner: [Operations and Conformance](../11-operations-and-conformance.md). Future reference path: `reference/operations-and-conformance.md`. |
| "Was it checked independently?" | Verification | Distinguishes self-checks from detached checks. | [Use: User Guide](../use/user-guide.md); current owner: [Kernel Spec](../03-kernel-spec.md). Future reference path: `reference/kernel.md`. |
| "Did a person inspect the experience?" | Manual QA | Covers UX, copy, accessibility, visual quality, and workflow judgment that tests may miss. | [Use: User Guide](../use/user-guide.md); current owner: [Design Quality Policy Pack](../08-design-quality-policy-pack.md). Future reference path: `reference/design-quality-policies.md`. |
| "Is the result good enough?" | Acceptance | Records the user's final judgment when the task path requires it. | [Use: User Guide](../use/user-guide.md); current owner: [Kernel Spec](../03-kernel-spec.md). Future reference path: `reference/kernel.md`. |
| "What uncertainty remains?" | Residual Risk | Makes known limitations visible before close or acceptance. | [Use: User Guide](../use/user-guide.md); current owner: [Kernel Spec](../03-kernel-spec.md). Future reference path: `reference/kernel.md`. |
| "Can we call this done?" | Close | Finishes the Task only when the relevant blockers have been handled. | Current owners: [Kernel Spec](../03-kernel-spec.md) and [Agent Integration](../09-agent-integration.md). Future reference paths: `reference/kernel.md`, `reference/agent-integration.md`. |

## Where to go next

- Read [Concepts](concepts.md) for a tighter vocabulary after the examples.
- Read [User Guide](../use/user-guide.md) to learn the phrases users can say during a Harness session.
- For strict behavior for Task, Change Unit, Decision Packet, Approval, Write Authorization, Acceptance, Residual Risk, and Close during the redesign, current owner: [Kernel Spec](../03-kernel-spec.md). Future reference path: `reference/kernel.md`.
- Read [Agent Session Flow](../use/agent-session-flow.md) to understand how an agent should present these flows without exposing unnecessary internals.
