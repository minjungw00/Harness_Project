# Legacy v1 Documentation Archive

These files are archived source material for the v2 documentation rewrite.

They are not active canonical documentation. Later rewrite batches should use them as migration inputs, then write canonical content to the v2 target files under `docs/`.

See `docs/appendix/D-migration-notes.md` for the active old-to-new mapping.

