# Codex Working Rules

This repo is currently in documentation redesign mode.

- Do not implement the harness server yet.
- Work on `docs/en` first; update `docs/ko` only when explicitly requested.
- Keep source-of-truth boundaries strict: `state.sqlite`/artifact store/projections.
- Do not duplicate schemas or DDL outside owner docs.
- Use small batches and report changed files.
